# 通用规范合规标记（TBD）

> 本文件**只做标记**，不做整改。整改由本项目**自己**在后续重新读取通用规范后，结合本项目落盘记录自行完成。
> 评审基线：`/ws/tasks/doc/rules.md` v3.3（**以最新版为准**）｜评审日期：2026-09-13
> 评审范围：`agent/patent-disclosure-skill/` 全部
> 完成整改后，请把本文件移入 `done/` 或删除，并在本项目 History 记一笔。

## 本项目定性（先说清楚，影响整改方式）

本项目是**外部引入**的 skill（origin = `https://github.com/handsomestWei/patent-disclosure-skill`），
不是自建任务目录。§6.5 规定「外部引入的 skill 以锁文件（如 `skills-lock.json`）记录来源与哈希」。
**因此整改方式应是"登记来源 + 只加薄封装"，而非把上游仓库改造成我们的任务目录结构**——
改造上游体例会破坏可升级性（§5.6 精神：最小化对上游既有结构的改动）。

## 标记项

- [ ] **P1 ｜ §6.5：未以锁文件登记来源与哈希**
  - 现状：本目录直接是上游 clone（1 笔提交），`/ws/tasks/tc4as/skills-lock.json` 是另一处的锁文件；本目录自身无来源/哈希登记。
  - 依据：§6.5「外部引入的 skill 以锁文件（如 `skills-lock.json`）记录来源与哈希」。
  - 待办：补锁文件条目（仓库 URL + 引入时的 commit 哈希 + 日期），或在所属任务的锁文件中登记本目录。

- [ ] **P2 ｜ §6.5：自建部分与上游部分未分离**
  - 现状：整目录为上游体例，我们的使用约定无处安放（无 `docs/AI-USAGE.md`）。
  - 依据：§6.5「skill 只做编排：事实细节指向文档，不复制内容」；§6.3 能力包双读者文档。
  - 待办：在**不改上游文件**的前提下，另加我们的使用说明（如 `docs/AI-USAGE.md` 或 README 附加节）。

- [ ] **P3 ｜ §4.1 / §1.1：缺任务三件套与标准目录**
  - 现状：`docs/` 下只有 8 张图片；无 `DESIGN.md`/`PROGRESS.md`/`History.md`；无 `todo/`、`done/`、`tools/`（脚本在 `scripts/`）；`.gitignore` 缺 `build/`。
  - 说明：**若按上节定性为"外部 skill"则本条多数不适用**——登记与薄封装即可，不必照搬任务目录规格。
  - 待办：先定性质，再决定是否需要本条。

## 已符合项（留档，便于对照）

- 有 README（上游体例）✓｜有 origin 远端 ✓｜独立 git 仓库 ✓

## 备注

- 上游为**第三方**，`ls -a` 变更与合并策略需与 §5.4（上游改造纪律）一致：能不改就不改。
